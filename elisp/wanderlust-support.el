;; Provide support for Wanderlust (verision ...)

(setq load-path
      (append
       (list
	(expand-file-name "/usr/local/share/emacs/site-lisp/emu")
	(expand-file-name "/usr/local/share/emacs/site-lisp/apel"))
       load-path))


